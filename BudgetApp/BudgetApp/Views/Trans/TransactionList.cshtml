@model IEnumerable<BudgetApp.Models.Trans>
@*Set the language*@
@{LanguageControl l = new LanguageControl("DK");}
@{
    ViewData["Title"] = "TransactionList";
}

<p>
    <a asp-action="Create">@l.CreateNewButton</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@l.Description</th>
            <th>@l.AccountName</th>
            <th>@l.SubCategoryName</th>
            <th class="text-right">@l.Amount</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account.AccountName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subcategory.SubcategoryName)
            </td>
            <td class="text-right
                @( item.Amount > 0? "text-forest" : item.Amount == 0? "text-black" : "text-red")">
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransId">
                    <img src="~/img/bootstrap-icons-1.8.1/pencil.svg" aria-hidden="true"/>
                     @*@l.EditButton*@
                </a> |
                 <a asp-action="Details" asp-route-id="@item.TransId">
                     <img src="~/img/bootstrap-icons-1.8.1/trash3.svg" aria-hidden="true"
                     alt="@l.DetailsButton"/>
                     @*@l.DeleteButton*@
                 </a> |
                 <a asp-action="Delete" asp-route-id="@item.TransId">
                     <img src="~/img/bootstrap-icons-1.8.1/trash3.svg" aria-hidden="true"/>
                     @*@l.DeleteButton*@
                 </a>
            </td>
        </tr>
}
    </tbody>
</table>
